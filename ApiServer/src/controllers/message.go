package controllers

import (
	// "ApiServer/commdef"
	// "encoding/base64"
	// "fmt"
	"github.com/astaxie/beego"
	// "github.com/astaxie/beego/orm"
	// "ApiServer/models"
)

type MsgController struct {
	beego.Controller
}

func (e *MsgController) URLMapping() {
	// m.Mapping("GetNewEventCount", e.GetNewEventCount)
}
